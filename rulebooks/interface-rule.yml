---

- name: Port State Event from Arista
  hosts: all
  sources:
   - ansible.eda.kafka:
       host: streaming.ericlab.local
       port: 9092
       topic: telegraf2
  rules:
   - name: Test
     condition: event.fields.admin_status == "DOWN"
     action:
       run_job_template:
         name: EDA_Admin
